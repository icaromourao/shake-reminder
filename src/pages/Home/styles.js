import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
    position: 'relative',
  },
  viewContainer: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 16,
  },
});

export default styles;
